version: 1
frontend:
  phases:
    preBuild:
-     commands:
-       - npm ci
+     commands:
+       # install & switch to Node 20
+       - nvm install 20
+       - nvm use 20
+       # now install your deps
+       - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
